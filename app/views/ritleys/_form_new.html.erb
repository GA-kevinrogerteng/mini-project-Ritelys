	<%= form_for :ritley, method: :post, url: ritleys_path do |f| %>
		<%= label_tag(:name, "Name of Site") %> <%= f.text_field :name%><br>
		<%= label_tag(:url_link, "URL of Link") %> <%= f.text_field :url_link %><br>
		<%= f.hidden_field :random_string, value: @random%>
		<%= f.submit "Generate Ritley" %>
	<%end%>